// Sort an array the worst way possible
fun bubbleSort(arr int[]) {
	for i int = 0; i < arr.len; ++i {
		for j int = 0; j < arr.len-1-i; ++j {
			if [j]arr > [j+1]arr {
				tmp int = [j]arr;
				[j]arr = [j+1]arr;
				[j+1]arr = tmp;
			}
		}
	}
}
